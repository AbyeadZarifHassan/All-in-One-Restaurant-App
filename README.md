# RMEDU Coffee Shop — Streamlit Invoice App ☕🧾

A **Streamlit-based coffee shop billing system** that can:

-  Generate invoices with **VAT (9%)**
- Save invoices as `.txt` files inside an `invoices/` folder
- Show all saved invoices
- Search invoices by **customer name + date**
- Display invoice items in a clean **table (Pandas DataFrame)**

---

## Preview

**Main features inside the app**
1. **Generate Invoice**
2. **Show all Invoices**
3. **Search Invoice**

The app also includes a **background image theme** using custom CSS.

---

## Project Structure

